# .tick/

This directory is auto-managed by the [Daily tick](../.github/workflows/daily-tick.yml) GitHub Actions workflow.

The `last-sync.txt` file is updated once per business day to keep the contribution graph active. No code, no logic — just a marker file.

You can safely ignore this directory.
